import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.assetsync.monitor',
    appName: 'Asset Sync Companion',
    webDir: 'dist',
    bundledWebRuntime: false
};

export default config;
